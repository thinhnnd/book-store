export class User {
  //access modifier, getter seter neu can thiet
  // purpose: unit test

  _id?: string;
  email: string;
  password: string;
  firstName?: string;
  lastName?: string;
  role?: string;
}

// soft delete user
